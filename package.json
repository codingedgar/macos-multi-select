{
    "name": "macos-multi-select",
    "version": "0.2.0",
    "description": "Given an Index, and an Action, return an array of selected keys with the same behaviour of macOS finder list view selection.",
    "main": "dist/index.js",
    "repository": "git@github.com:codingedgar/macos-multi-select.git",
    "author": "codingedgar <codingedgar@gmail.com>",
    "license": "MIT",
    "scripts": {
        "test": "jest",
        "watch-test": "jest --watch",
        "build": "rm -r dist && tsc",
        "prepare": "yarn build",
        "build-demo": "parcel demo/index.html -d docs --port 3000 --hmr-port 3001",
        "watch-demo": "parcel demo/index.html --open -d docs --port 3000 --hmr-port 3001"
    },
    "bugs": {
        "url": "https://github.com/davidkpiano/xstate/issues"
    },
    "devDependencies": {
        "@types/jest": "^27.0.0",
        "@types/ramda": "types/npm-ramda#dist",
        "fast-check": "^2.17.0",
        "jest": "^27.0.6",
        "parcel-bundler": "^1.12.5",
        "ts-jest": "^27.0.4",
        "typescript": "^4.3.5"
    },
    "dependencies": {
        "ramda": "^0.27.1"
    }
}
